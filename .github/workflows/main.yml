      - name: Run script
        env:
          GIST_TOKEN: ${{ secrets.GIST_TOKEN }}
        run: |
          echo "--- ГДЕ МЫ СЕЙЧАС ---"
          pwd
          echo "--- ЧТО ТУТ ЛЕЖИТ ---"
          ls -R
          echo "--- ЗАПУСК С КОРНЯ ---"
          python3 main.py
