name: Debug

on:
  workflow_dispatch:

jobs:
  check:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: List files
        run: |
          echo "--- ГДЕ МЫ ---"
          pwd
          echo "--- ЧТО В ПАПКЕ ---"
          ls -R
